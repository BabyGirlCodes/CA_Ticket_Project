package CLI;
public class UserMenu {
    public static void show(String username) {
        System.out.println("\n👤 Welcome, " + username + " (User)");
        // You can add: Create ticket, View tickets, etc.
    }
}
