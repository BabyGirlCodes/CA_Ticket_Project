package CLI;

public class AgentMenu {
    public static void show(String username) {
        System.out.println("\n🛠️ Welcome, " + username + " (Agent)");
        // You can add: View unassigned tickets, Assign tickets, etc.
    }
}
